import React from "react"
import { PizzaCard } from "@molecules/PizzaCard"

export const HomePage: React.FC = () => {
    return (
        <div>
            <main>
                <PizzaCard />
            </main>
        </div>
    )
}